{%- doc -%}
  Legacy Frame Finish line-item property UI.

  Frame Finish is now a real Shopify variant option rendered by
  `variant-main-picker` with `described_layout == 'finish'`. This snippet is
  kept only so existing theme references do not break; it is no longer rendered
  from the product form.
{%- enddoc -%}
